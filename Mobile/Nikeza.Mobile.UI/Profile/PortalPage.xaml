<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:Nikeza.Mobile.UI.Converters"
             x:Class="Nikeza.Mobile.UI.PortalPage">

    <ContentPage.Resources>
        <ResourceDictionary>

            <converters:NameConverter x:Key="NameConverter" />

            <Style x:Key="ButtonStyle" TargetType="Button" />
            
            <Style x:Key="ImageStyle"  TargetType="Image">
                <Setter Property="Source" Value="http://www.ngu.edu/myimages/silhouette2230.jpg" />
                <Setter Property="WidthRequest"    Value="100" />
                <Setter Property="HeightRequest"   Value="100" />
                <Setter Property="VerticalOptions" Value="Start" />
            </Style>

            <Style x:Key="RecentImageStyle"  TargetType="Image" BasedOn="{StaticResource ImageStyle}">
                <Setter Property="WidthRequest"    Value="60" />
                <Setter Property="HeightRequest"   Value="60" />
            </Style>

        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Image  Grid.Column="0" Grid.Row="0" Style="{StaticResource ImageStyle}" Source="{Binding ProfileImage}"  />
        <Button Grid.Column="0" Grid.Row="1" Style="{StaticResource ButtonStyle}" Text="Latest" />
        <Button Grid.Column="0" Grid.Row="2" Style="{StaticResource ButtonStyle}" Text="Portfolio" />
        <Button Grid.Column="0" Grid.Row="3" Style="{StaticResource ButtonStyle}" Text="Link" />
        <Button Grid.Column="0" Grid.Row="4" Style="{StaticResource ButtonStyle}" Text="Subscriptions" />
        <Button Grid.Column="0" Grid.Row="5" Style="{StaticResource ButtonStyle}" Text="Followers" />
        <Button Grid.Column="0" Grid.Row="6" Style="{StaticResource ButtonStyle}" Text="Members" />

        <Grid Grid.Column="1" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <ListView Grid.Column="0" Grid.Row="0" ItemsSource="{Binding Subscriptions}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" 
                                   Grid.Row=   "0" 
                                   Style= "{StaticResource RecentImageStyle}"
                                   Source="{Binding Profile.ImageUrl}"/>

                                <Label Grid.Column="1" 
                                       Grid.Row=   "0" 
                                       Text="{Binding Profile, Converter={StaticResource NameConverter}}" />

                                <ListView Grid.Column="1" 
                                          Grid.Row=   "1" 
                                          ItemsSource="{Binding RecentLinks}" >
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <ViewCell>
                                                <Label Text="{Binding Title}" />
                                            </ViewCell>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
    
</ContentPage>